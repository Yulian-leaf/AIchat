# Milestones

## M0：静态 UI（完成）
- 单页聊天 UI：消息列表 + 输入框 + 发送按钮

## M1：HTTP API
- `POST /api/chat`：前端 -> 后端 -> 返回回复

## M2：WebSocket
- `GET /ws`：实时消息通道（JSON 协议）

## M3（可选）：接入 AI API
- 把占位回复替换为 Deepseek/OpenAI 等
